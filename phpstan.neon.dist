includes:
    - config/stubFiles.neon
    - config/extensions.neon

parameters:
    level: max
    # make phpstan results consisten across php versions
    phpVersion: 80019 # PHP 8.019

    paths:
        - src/

    bootstrapFiles:
        - bootstrap.php
